import { motion } from "framer-motion";
import { Navigate } from "react-router-dom";
import { RotateCcw, Home } from "lucide-react";
import AnimatedBackground from "../components/AnimatedBackground.jsx";
import Logo from "../components/Logo.jsx";
import ScoreReveal from "../components/ScoreReveal.jsx";
import ResultSummary from "../components/ResultSummary.jsx";

export default function Result({ result, formData, onRestart, onHome }) {
  if (!result) return <Navigate to="/" replace />;

  const score = result.predicted_mental_health_score;

  return (
    <motion.main
      exit={{ opacity: 0 }}
      transition={{ duration: 0.3 }}
      className="relative min-h-screen overflow-hidden bg-ink-900 px-6 py-12"
    >
      <AnimatedBackground variant="quiet" />

      <div className="relative z-10 mx-auto flex max-w-lg flex-col items-center">
        <Logo size="sm" className="mb-10" />

        <motion.p
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 0.2 }}
          className="mb-2 font-display text-sm italic text-mist-400"
        >
          Your Mindexia score
        </motion.p>

        <motion.div
          initial={{ opacity: 0, scale: 0.92 }}
          animate={{ opacity: 1, scale: 1 }}
          transition={{ delay: 0.3, duration: 0.6, ease: [0.22, 1, 0.36, 1] }}
        >
          <ScoreReveal score={score} />
        </motion.div>

        <motion.p
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 0.9 }}
          className="mt-4 max-w-sm text-center text-sm text-mist-400"
        >
          A snapshot of the patterns you shared. Generated by a machine-learning model — not a
          medical diagnosis.
        </motion.p>

        <motion.div
          initial={{ opacity: 0, y: 16 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 1.1, duration: 0.6 }}
          className="mt-10 w-full"
        >
          <ResultSummary formData={formData} />
        </motion.div>

        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 1.4 }}
          className="mt-10 flex flex-wrap items-center justify-center gap-3"
        >
          <button
            onClick={onRestart}
            className="flex items-center gap-2 rounded-full bg-mist-100 px-5 py-3 text-sm font-medium text-ink-950 transition-transform hover:scale-105"
          >
            <RotateCcw size={15} />
            Take another assessment
          </button>
          <button
            onClick={onHome}
            className="flex items-center gap-2 rounded-full border border-mist-400/20 px-5 py-3 text-sm text-mist-300 transition-colors hover:text-mist-100"
          >
            <Home size={15} />
            Back to Mindexia
          </button>
        </motion.div>
      </div>
    </motion.main>
  );
}
